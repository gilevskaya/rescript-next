{
  "name": "rescript-nextjs",
  "version": "0.1.0",
  "scripts": {
    "clean": "rescript clean -with-deps",
    "dev": "concurrently \"rescript clean -with-deps && rescript build -w\" \"next dev\"",
    "build": "rescript clean -with-deps && rescript build -with-deps && next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^12.0.10",
    "next-connect": "^0.12.1",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "license": "MIT",
  "keywords": [],
  "description": "",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "@rescript/react": "^0.10.3",
    "rescript": "^9.1.4"
  }
}